#include "ForgePlayDefaults.xcconfig"

PRODUCT_NAME = $(FORGEPLAY_PRODUCT_NAME)
PRODUCT_BUNDLE_IDENTIFIER = $(FORGEPLAY_APP_BUNDLE_ID)
INFOPLIST_FILE = Sources/ForgePlay/Info.plist
CODE_SIGN_ENTITLEMENTS = Sources/ForgePlay/ForgePlay.entitlements
GENERATE_INFOPLIST_FILE = NO
ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon
CODE_SIGN_INJECT_BASE_ENTITLEMENTS[config=Release] = NO
