import Foundation

enum SupporterRecognitionCatalog {
    // This list is curated by the ForgePlay maintainer. Add only the exact
    // public names that should be engraved on the in-app supporter plaque.
    static let names: [String] = [
    ]
}
